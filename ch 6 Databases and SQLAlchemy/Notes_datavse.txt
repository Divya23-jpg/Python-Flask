Introduction to databases
         |
 SQLALchemy Basics
         |
1.Creating Modules   2. Data Types and Relationships  3. Queries
                                                            |
                                    1. .add()  2. .commit()  3. .filter()   4. .all()  5. .get()





Problems:
1. Data gets lost in memory (Data lose after the refresh and not stored anywhere)
2. File storage Unreliable (We cant Search , filter)
3. data increase but not handle


Solution:
1. Data permanently stores.
2. We can fimd , filter, search anything
3. Data can be handle even millions of data 


What is  Databases
Databases are structured way to store data in the form of table (row and columns in MYSQL)


What is SQL Lite
1.It is a file based database Engine
2. It built in Python no need to install from anywhere
3.We can work with small/medium level flask
4. Stored as data.db

Benifits in flask 
1.No setup needed
2.all data stored in file to easy to manage
3. Great choice to build small app



Disadvanatages of SQLite
1. In big companies there is no use of SQLite because cannot handle  large amount data
2. Not supported full featured
